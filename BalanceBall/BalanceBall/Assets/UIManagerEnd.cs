using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIManagerEnd : MonoBehaviour
{
    public TextMesh Score2;

    //public Text Text { get => text; set => text = value; }

    // Start is called before the first frame update
    void Start()
    {
        
    }
    /*
    // Update is called once per frame
    void Awake() { Score2 = GetComponent(); score = PlayerPrefs.GetInt("Score"); }

    void Update()
    {
        Score2 = "Score: " + score; PlayerPrefs.SetInt("Score", score);

    }
    */
}
